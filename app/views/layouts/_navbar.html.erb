<nav class="navbar is-transparent">
  <div class="navbar-brand">
    <a class="navbar-item has-text-weight-bold" href="<%= root_path %>">
      CLUBHOUSE 
    </a>
    <div class="navbar-burger burger" data-target="navbarExampleTransparentExample">
      <span></span>
      <span></span>
      <span></span>
    </div>
  </div>

  <div id="navbarExampleTransparentExample" class="navbar-menu">
    <div class="navbar-start">
      <%= link_to "Home", root_path, class: "navbar-item"%>
      <%= link_to("Users", users_path, class: "navbar-item") if logged_in? %>
      <% if logged_in? %>
        <div class="navbar-item has-dropdown is-hoverable">
          <a class="navbar-link" href="">
            Account
          </a>
          <div class="navbar-dropdown is-boxed">
            <%= link_to "Profile", current_user, class: "navbar-item" %>
            <%= link_to "Settings", edit_user_path(current_user), class: "navbar-item"%>
          </div>
        </div>
        <div class="navbar-item">
          <p class="control">
            <%= link_to "New post", new_post_path, class: "button is-primary"%>
          </p>
        </div>
      <% end %>
    </div>

    <div class="navbar-end">
      <% if logged_in? %>
      <div class="navbar-item">
        <p class="control">
          <%= link_to "Logout", logout_path, class: "button is-primary",
                                            method: :delete, data: {confirm: "Do you want to logout?"} %>
        </p>
      </div>
      <% else %>
      <div class="navbar-item">
        <p class="control">
          <%= link_to "Signup", new_user_path, class: "button is-primary"%>
        </p>
      </div>
      <div class="navbar-item">
        <p class="control">
          <%= link_to "Login", login_path, class: "button is-primary"%>
        </p>
      </div>
      <% end %>
    </div>
  </div>
</nav>